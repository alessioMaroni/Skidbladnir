project Time is
   for Languages use ("Ada");
   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Library_Dir use "lib";
   for Library_Name use "ada_time";
   for Library_Kind use "static";

   package Compiler is
      for Default_Switches ("Ada") use (
         "-O2",
         "-gnatp",
         "-mno-red-zone"
      );
   end Compiler;
end Time;